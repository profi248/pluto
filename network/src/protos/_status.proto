syntax = "proto3";

message ErrorResponse {
  enum ErrorType {
    SERVER_ERROR = 0;
    PUBKEY_LENGTH_INVALID = 1;
    CHALLENGE_RESPONSE_INVALID = 2;
    SESSION_INVALID = 3;
  };
  ErrorType error = 2;
  string message = 3;
}
