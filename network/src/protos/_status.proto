syntax = "proto3";

message Error {
  enum Error {
    SERVER_ERROR = 0;
    PUBKEY_LENGTH_INVALID = 1;
    CHALLENGE_RESPONSE_INVALID = 2;
    SESSION_INVALID = 3;
  };
  Error error = 2;
  string message = 3;
}
